package jmusichub.util;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

import musichub.main.*;
import musichub.util.*;
import musichub.business.*;

class JMusicHubActiveViewTest {
	@Test
	void TestValidator() {

		// PasswordValidator pv = new PasswordValidator();
		// //less than 6
		// assertEquals(pv.validatePassword("12345"), false);
		// //exactly 6
		// assertEquals(pv.validatePassword("123456"), true);
		// //more than 20
		// assertEquals(pv.validatePassword("123456abcdeo pç0°)345lskdfdllke23"), false);
		// //exactly 20
		// assertEquals(pv.validatePassword("123456abcdeo pç0°)34"), true);
		// //no number
		// assertEquals(pv.validatePassword("abc$deo pç°)"), false);
	}

}
